{
  "name": "@aglit-ai/gmail-rl-delivery-6",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Stateful Gmail RL environment delivery for Google aglit, version 6.",
  "scripts": {
    "dev": "node src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test",
    "typecheck": "node --check src/server.mjs && node --check src/state-store.mjs && node --check public/app.js && node --check scripts/local-validation.mjs",
    "validate:local": "node scripts/local-validation.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
